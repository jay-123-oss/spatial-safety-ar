# Copy this file to local.properties for a local Android build.
# local.properties itself is ignored and must not be committed.
sdk.dir=/absolute/path/to/android-sdk

# The VLM is offline-first. Place the SmolVLM2-500M runtime/artifact in the
# configured local model assets location when the Android runtime is enabled.
